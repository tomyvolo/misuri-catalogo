export type Product = {
  id: number
  name: string
  price: number
  category: string
  description: string
  size: string
  image_url: string
}

export type Category = {
  name: string
  slug: string
}
